[package]
name = "overflow"
version = "0.1.0"
edition = "2021"
[profile.release]
strip = "debuginfo"
opt-level = 1               # Use slightly better optimizations.
overflow-checks = false     # Disable integer overflow checks.

